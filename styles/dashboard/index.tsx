import { Box } from '@mui/material';

import styled from 'styled-components';

const Container = styled(Box)`
  padding: 1rem;
`;

const DashboardStyle = { Container };

export default DashboardStyle;
