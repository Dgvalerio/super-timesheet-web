import styled from 'styled-components';

const Container = styled.main`
  margin: auto;
  padding: 4rem;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 1rem;
`;

const UserCreateStyle = { Container };

export default UserCreateStyle;
